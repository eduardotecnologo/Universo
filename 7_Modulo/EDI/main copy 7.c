#include <stdio.h>
#include <stdlib.h>

int main()
{
  // Definindo variáveis
  int i, j;
  float x;
  // Entrada de dados
  scanf("%d", &i);
  scanf("%d%f", &j, &x);
  // Exibição de dados
  printf("I= %d\n", i);
  printf("J= %d\nX=%f\n", j, x);
  return 0;
}